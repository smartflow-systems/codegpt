{
  "name": "codegpt-smartflow",
  "version": "1.0.0",
  "description": "CodeGPT with SmartFlow Systems theme and auto-sync",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "sync": "bash scripts/auto-sync.sh",
    "pull": "git pull origin main --no-edit",
    "push": "git push origin main",
    "auto-commit": "git add . && git commit -m 'chore: auto-commit [Replit]' || true",
    "sync-all": "npm run auto-commit && npm run sync"
  },
  "keywords": [
    "smartflow",
    "codegpt",
    "replit"
  ],
  "author": "SmartFlow Systems",
  "license": "MIT",
  "engines": {
    "node": ">=18.0.0"
  }
}
